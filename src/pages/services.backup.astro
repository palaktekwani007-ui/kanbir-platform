---
// Services Page - Kanbir Solutions
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="Expert Services — Accounting, Taxation, ERP Integration, Business Advisory & R&D Claims Consulting" />
  <title>Services | Kanbir Solutions</title>
  
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet" />

  <style>
    /* ═════════════════════════════════════════════════════════ */
    /* COLOR TOKENS - Light & Dark Blue Mix                     */
    /* ═════════════════════════════════════════════════════════ */
    :root {
      --navy-dark:    #050e27;
      --navy:         #0b1d4a;
      --blue-deep:    #1a56db;
      --blue:         #2563eb;
      --blue-mid:     #3b82f6;
      --blue-light:   #60a5fa;
      --blue-lighter: #93c5fd;
      --blue-pale:    #dbeafe;
      --blue-ice:     #eff6ff;
      --white:        #ffffff;
      --gray-100:     #f3f4f6;
      --gray-700:     #374151;
      --gray-800:     #1f2937;
      --text-dark:    #1f2937;
      --text-light:   #6b7280;
      --bg-light:     #ffffff;
      --bg-alt:       #f9fafb;
      
      --shadow-sm:    0 1px 2px rgba(26,86,219,0.05);
      --shadow-md:    0 4px 12px rgba(26,86,219,0.1);
      --shadow-lg:    0 10px 30px rgba(26,86,219,0.15);
      --shadow-xl:    0 20px 50px rgba(26,86,219,0.2);
      
      --transition:   all 0.3s cubic-bezier(0.4,0,0.2,1);
    }

    /* ═════════════════════════════════════════════════════════ */
    /* RESET & BASE                                             */
    /* ═════════════════════════════════════════════════════════ */
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    html { scroll-behavior: smooth; }
    body {
      font-family: 'Inter', sans-serif;
      background: var(--bg-light);
      color: var(--text-dark);
      line-height: 1.6;
      overflow-x: hidden;
    }
    a { text-decoration: none; color: inherit; }
    img { display: block; max-width: 100%; }

    /* ═════════════════════════════════════════════════════════ */
    /* ANIMATIONS                                               */
    /* ═════════════════════════════════════════════════════════ */
    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(30px); }
      to { opacity: 1; transform: translateY(0); }
    }

    @keyframes slideInLeft {
      from { opacity: 0; transform: translateX(-40px); }
      to { opacity: 1; transform: translateX(0); }
    }

    @keyframes slideInRight {
      from { opacity: 0; transform: translateX(40px); }
      to { opacity: 1; transform: translateX(0); }
    }

    @keyframes textGlow {
      0%, 100% { text-shadow: 0 0 20px rgba(37,99,235,0.2); }
      50% { text-shadow: 0 0 40px rgba(37,99,235,0.4); }
    }

    @keyframes pulse {
      0%, 100% { opacity: 1; }
      50% { opacity: 0.5; }
    }

    @keyframes float {
      0%, 100% { transform: translateY(0px); }
      50% { transform: translateY(-10px); }
    }

    /* ═════════════════════════════════════════════════════════ */
    /* NAVBAR                                                   */
    /* ═════════════════════════════════════════════════════════ */
    .navbar {
      position: sticky;
      top: 0;
      z-index: 100;
      background: rgba(255,255,255,0.92);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border-bottom: 1px solid rgba(26,86,219,0.1);
      box-shadow: var(--shadow-sm);
      transition: var(--transition);
    }

    .nav-inner {
      max-width: 1400px;
      margin: 0 auto;
      padding: 0 32px;
      height: 70px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 40px;
    }

    .nav-logo {
      display: flex;
      align-items: center;
      gap: 12px;
      font-size: 1.3rem;
      font-weight: 800;
      background: linear-gradient(135deg, var(--blue) 0%, var(--blue-mid) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      white-space: nowrap;
      transition: var(--transition);
    }

    .nav-logo:hover { transform: scale(1.05); }

    .nav-links {
      display: flex;
      gap: 8px;
      flex: 1;
      justify-content: center;
    }

    .nav-links a {
      padding: 8px 16px;
      border-radius: 10px;
      font-size: 0.95rem;
      font-weight: 500;
      color: var(--text-light);
      transition: var(--transition);
      position: relative;
    }

    .nav-links a::before {
      content: "";
      position: absolute;
      bottom: 0;
      left: 50%;
      width: 0;
      height: 2px;
      background: linear-gradient(90deg, var(--blue), var(--blue-light));
      transition: all 0.3s ease;
      transform: translateX(-50%);
    }

    .nav-links a:hover {
      color: var(--blue);
      background: var(--blue-ice);
    }

    .nav-links a:hover::before { width: 60%; }

    .nav-actions {
      display: flex;
      gap: 16px;
      align-items: center;
    }

    .btn-nav {
      padding: 10px 20px;
      border-radius: 10px;
      font-weight: 600;
      border: none;
      cursor: pointer;
      transition: var(--transition);
      font-size: 0.9rem;
    }

    .btn-nav.secondary {
      border: 2px solid var(--blue-light);
      color: var(--blue);
      background: transparent;
    }

    .btn-nav.secondary:hover {
      background: var(--blue-ice);
      border-color: var(--blue-mid);
    }

    .btn-nav.primary {
      background: linear-gradient(135deg, var(--blue) 0%, var(--blue-mid) 100%);
      color: white;
      box-shadow: var(--shadow-md);
    }

    .btn-nav.primary:hover {
      transform: translateY(-2px);
      box-shadow: var(--shadow-lg);
    }

    /* ═════════════════════════════════════════════════════════ */
    /* HERO SECTION                                             */
    /* ═════════════════════════════════════════════════════════ */
    .hero {
      min-height: 100vh;
      display: grid;
      grid-template-columns: 1fr 1fr;
      align-items: center;
      gap: 60px;
      padding: 120px 60px;
      background: linear-gradient(135deg, var(--blue-ice) 0%, var(--white) 100%);
      position: relative;
      overflow: hidden;
    }

    .hero::before {
      content: "";
      position: absolute;
      top: -50%;
      right: -20%;
      width: 600px;
      height: 600px;
      background: radial-gradient(circle, rgba(37,99,235,0.15), transparent 70%);
      border-radius: 50%;
      filter: blur(60px);
      animation: float 8s ease-in-out infinite;
    }

    .hero::after {
      content: "";
      position: absolute;
      bottom: -30%;
      left: -10%;
      width: 500px;
      height: 500px;
      background: radial-gradient(circle, rgba(96,165,250,0.1), transparent 70%);
      border-radius: 50%;
      filter: blur(50px);
      animation: float 10s ease-in-out infinite 1s;
    }

    .hero-content {
      position: relative;
      z-index: 2;
    }

    .hero-badge {
      display: inline-block;
      padding: 12px 24px;
      background: linear-gradient(135deg, rgba(37,99,235,0.1), rgba(96,165,250,0.05));
      border: 1px solid var(--blue-light);
      border-radius: 50px;
      font-size: 0.9rem;
      font-weight: 600;
      color: var(--blue);
      margin-bottom: 20px;
      animation: slideInLeft 0.8s ease-out;
    }

    .hero h1 {
      font-size: clamp(2.5rem, 5vw, 4.5rem);
      line-height: 1.2;
      font-weight: 900;
      letter-spacing: -1.5px;
      margin-bottom: 20px;
      background: linear-gradient(135deg, var(--navy) 0%, var(--blue) 50%, var(--blue-light) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      animation: fadeInUp 0.8s ease-out 0.1s both;
    }

    .hero p {
      font-size: 1.15rem;
      line-height: 1.8;
      color: var(--text-light);
      max-width: 550px;
      margin-bottom: 40px;
      animation: fadeInUp 0.8s ease-out 0.2s both;
    }

    .hero-buttons {
      display: flex;
      gap: 20px;
      flex-wrap: wrap;
      animation: fadeInUp 0.8s ease-out 0.3s both;
    }

    .btn {
      padding: 14px 32px;
      border-radius: 12px;
      font-weight: 700;
      font-size: 1rem;
      border: none;
      cursor: pointer;
      transition: var(--transition);
      position: relative;
      overflow: hidden;
    }

    .btn.primary {
      background: linear-gradient(135deg, var(--blue) 0%, var(--blue-mid) 100%);
      color: white;
      box-shadow: var(--shadow-lg);
    }

    .btn.primary:hover {
      transform: translateY(-4px);
      box-shadow: var(--shadow-xl);
    }

    .btn.secondary {
      border: 2px solid var(--blue);
      color: var(--blue);
      background: transparent;
    }

    .btn.secondary:hover {
      background: var(--blue-ice);
    }

    .hero-visual {
      position: relative;
      z-index: 3;
      animation: slideInRight 0.8s ease-out 0.4s both;
    }

    .visual-card {
      background: linear-gradient(135deg, var(--blue-pale) 0%, var(--blue-ice) 100%);
      border: 2px solid rgba(37,99,235,0.2);
      border-radius: 20px;
      padding: 40px;
      box-shadow: var(--shadow-lg);
      position: relative;
      overflow: hidden;
    }

    .visual-card::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: linear-gradient(135deg, rgba(255,255,255,0.4), transparent);
      pointer-events: none;
    }

    .visual-content {
      position: relative;
      z-index: 2;
      display: grid;
      gap: 20px;
    }

    .visual-item {
      display: flex;
      gap: 15px;
      align-items: flex-start;
      padding: 15px;
      background: rgba(255,255,255,0.6);
      border-radius: 12px;
      backdrop-filter: blur(10px);
    }

    .visual-icon {
      width: 50px;
      height: 50px;
      background: linear-gradient(135deg, var(--blue), var(--blue-mid));
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-size: 1.5rem;
      flex-shrink: 0;
    }

    .visual-text h3 {
      font-size: 1.05rem;
      font-weight: 700;
      color: var(--navy);
      margin-bottom: 5px;
    }

    .visual-text p {
      font-size: 0.9rem;
      color: var(--text-light);
    }

    /* ═════════════════════════════════════════════════════════ */
    /* SERVICES GRID                                            */
    /* ═════════════════════════════════════════════════════════ */
    .services-section {
      padding: 100px 60px;
      background: var(--bg-light);
    }

    .section-header {
      text-align: center;
      max-width: 700px;
      margin: 0 auto 70px;
    }

    .section-label {
      display: inline-block;
      padding: 8px 18px;
      background: linear-gradient(135deg, rgba(96,165,250,0.1), rgba(37,99,235,0.05));
      border: 1px solid var(--blue-light);
      border-radius: 50px;
      font-size: 0.85rem;
      font-weight: 600;
      color: var(--blue);
      margin-bottom: 15px;
    }

    .section-title {
      font-size: clamp(2rem, 4vw, 3.5rem);
      line-height: 1.3;
      font-weight: 900;
      color: var(--navy);
      margin-bottom: 15px;
      letter-spacing: -0.5px;
    }

    .section-title span {
      background: linear-gradient(135deg, var(--blue), var(--blue-light));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .section-description {
      font-size: 1.1rem;
      color: var(--text-light);
      line-height: 1.8;
    }

    .services-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
      gap: 30px;
      max-width: 1400px;
      margin: 0 auto;
    }

    .service-card {
      background: linear-gradient(135deg, var(--blue-ice) 0%, var(--white) 100%);
      border: 1px solid rgba(37,99,235,0.1);
      border-radius: 16px;
      padding: 40px;
      cursor: pointer;
      transition: var(--transition);
      position: relative;
      overflow: hidden;
      animation: fadeInUp 0.8s ease-out both;
    }

    .service-card:nth-child(1) { animation-delay: 0s; }
    .service-card:nth-child(2) { animation-delay: 0.1s; }
    .service-card:nth-child(3) { animation-delay: 0.2s; }
    .service-card:nth-child(4) { animation-delay: 0.3s; }
    .service-card:nth-child(5) { animation-delay: 0.4s; }
    .service-card:nth-child(6) { animation-delay: 0.5s; }

    .service-card::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: linear-gradient(135deg, rgba(37,99,235,0.05), transparent);
      opacity: 0;
      transition: opacity 0.4s ease;
    }

    .service-card:hover {
      transform: translateY(-8px);
      box-shadow: var(--shadow-xl);
      border-color: var(--blue-light);
      background: linear-gradient(135deg, var(--blue-pale) 0%, var(--blue-ice) 100%);
    }

    .service-card:hover::before { opacity: 1; }

    .service-icon {
      font-size: 3rem;
      margin-bottom: 20px;
      display: inline-block;
    }

    .service-card h3 {
      font-size: 1.4rem;
      font-weight: 800;
      color: var(--navy);
      margin-bottom: 15px;
      position: relative;
      z-index: 2;
    }

    .service-card p {
      font-size: 1rem;
      color: var(--text-light);
      line-height: 1.8;
      margin-bottom: 20px;
      position: relative;
      z-index: 2;
    }

    .service-link {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      color: var(--blue);
      font-weight: 700;
      font-size: 0.95rem;
      position: relative;
      z-index: 2;
      transition: var(--transition);
    }

    .service-link:hover {
      gap: 15px;
      color: var(--blue-mid);
    }

    /* ═════════════════════════════════════════════════════════ */
    /* FEATURES SECTION                                         */
    /* ═════════════════════════════════════════════════════════ */
    .features-section {
      padding: 100px 60px;
      background: linear-gradient(135deg, var(--blue-ice) 0%, var(--white) 100%);
    }

    .features-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 40px;
      max-width: 1400px;
      margin: 0 auto;
    }

    .feature-box {
      padding: 40px;
      background: var(--white);
      border: 1px solid rgba(37,99,235,0.1);
      border-radius: 16px;
      transition: var(--transition);
      position: relative;
      z-index: 1;
      animation: fadeInUp 0.8s ease-out both;
    }

    .feature-box:nth-child(1) { animation-delay: 0s; }
    .feature-box:nth-child(2) { animation-delay: 0.1s; }
    .feature-box:nth-child(3) { animation-delay: 0.2s; }

    .feature-box:hover {
      transform: translateY(-6px);
      box-shadow: var(--shadow-xl);
      border-color: var(--blue);
    }

    .feature-number {
      font-size: 3rem;
      font-weight: 900;
      background: linear-gradient(135deg, var(--blue), var(--blue-light));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      margin-bottom: 15px;
    }

    .feature-box h3 {
      font-size: 1.3rem;
      font-weight: 800;
      color: var(--navy);
      margin-bottom: 15px;
    }

    .feature-box p {
      font-size: 1rem;
      color: var(--text-light);
      line-height: 1.8;
    }

    /* ═════════════════════════════════════════════════════════ */
    /* MOUSE FOLLOWER EFFECT                                    */
    /* ═════════════════════════════════════════════════════════ */
    .mouse-follower {
      position: fixed;
      width: 30px;
      height: 30px;
      background: linear-gradient(135deg, var(--blue), var(--blue-light));
      border-radius: 50%;
      pointer-events: none;
      z-index: 1000;
      opacity: 0.6;
      mix-blend-mode: screen;
      box-shadow: 0 0 20px rgba(37,99,235,0.4);
      display: none;
    }

    .mouse-follower.active { display: block; }

    .mouse-glow {
      position: fixed;
      width: 100px;
      height: 100px;
      background: radial-gradient(circle, rgba(37,99,235,0.2), transparent);
      border-radius: 50%;
      pointer-events: none;
      z-index: 999;
      display: none;
      filter: blur(30px);
    }

    .mouse-glow.active { display: block; }

    /* ═════════════════════════════════════════════════════════ */
    /* CTA SECTION                                              */
    /* ═════════════════════════════════════════════════════════ */
    .cta-section {
      padding: 100px 60px;
      text-align: center;
      background: linear-gradient(135deg, var(--navy-dark) 0%, var(--blue-deep) 100%);
      color: white;
      position: relative;
      overflow: hidden;
    }

    .cta-section::before {
      content: "";
      position: absolute;
      top: -50%;
      right: -10%;
      width: 500px;
      height: 500px;
      background: radial-gradient(circle, rgba(96,165,250,0.15), transparent);
      border-radius: 50%;
      filter: blur(80px);
    }

    .cta-section::after {
      content: "";
      position: absolute;
      bottom: -50%;
      left: -10%;
      width: 500px;
      height: 500px;
      background: radial-gradient(circle, rgba(37,99,235,0.1), transparent);
      border-radius: 50%;
      filter: blur(80px);
    }

    .cta-content {
      position: relative;
      z-index: 2;
      max-width: 800px;
      margin: 0 auto;
    }

    .cta-section h2 {
      font-size: clamp(2rem, 4vw, 3.5rem);
      font-weight: 900;
      margin-bottom: 20px;
      letter-spacing: -1px;
    }

    .cta-section p {
      font-size: 1.2rem;
      margin-bottom: 40px;
      opacity: 0.9;
    }

    .cta-buttons {
      display: flex;
      gap: 20px;
      justify-content: center;
      flex-wrap: wrap;
    }

    .btn.light {
      background: white;
      color: var(--blue);
      box-shadow: var(--shadow-lg);
    }

    .btn.light:hover {
      transform: translateY(-4px);
      box-shadow: var(--shadow-xl);
    }

    /* ═════════════════════════════════════════════════════════ */
    /* RESPONSIVE                                               */
    /* ═════════════════════════════════════════════════════════ */
    @media (max-width: 1024px) {
      .hero { grid-template-columns: 1fr; padding: 80px 40px; gap: 40px; }
      .services-section { padding: 80px 40px; }
      .features-section { padding: 80px 40px; }
      .features-grid { grid-template-columns: 1fr 1fr; }
      .cta-section { padding: 80px 40px; }
      .nav-inner { padding: 0 20px; }
    }

    @media (max-width: 768px) {
      .nav-links { display: none; }
      .hero { grid-template-columns: 1fr; padding: 60px 24px; gap: 30px; }
      .services-grid { grid-template-columns: 1fr; }
      .features-grid { grid-template-columns: 1fr; }
      .hero-buttons { flex-direction: column; }
      .btn { width: 100%; }
      .services-section { padding: 60px 24px; }
      .features-section { padding: 60px 24px; }
      .cta-section { padding: 60px 24px; }
    }
  </style>
</head>

<body>
  <!-- ════════════════ NAVBAR ════════════════ -->
  <header class="navbar">
    <div class="nav-inner">
      <a href="/" class="nav-logo">
        <span>📊</span> Kanbir Solutions
      </a>
      
      <nav class="nav-links">
        <a href="/">Home</a>
        <a href="/services">Services</a>
        <a href="/about">About</a>
        <a href="/contact">Contact</a>
      </nav>

      <div class="nav-actions">
        <button class="btn-nav secondary">Portal</button>
        <button class="btn-nav primary">Get Started</button>
      </div>
    </div>
  </header>

  <!-- ════════════════ HERO ════════════════ -->
  <section class="hero">
    <div class="hero-content">
      <div class="hero-badge">🚀 Comprehensive Service Suite</div>
      
      <h1>Expert <span>Solutions</span> for<br/>Business Excellence</h1>
      
      <p>From accounting and taxation to ERP integration and R&D claims, we deliver precision-driven services that propel your business forward. Transform complexity into clarity with Kanbir Solutions.</p>
      
      <div class="hero-buttons">
        <button class="btn primary">Explore All Services</button>
        <button class="btn secondary">Schedule Demo</button>
      </div>
    </div>

    <div class="hero-visual">
      <div class="visual-card">
        <div class="visual-content">
          <div class="visual-item">
            <div class="visual-icon">📈</div>
            <div class="visual-text">
              <h3>Financial Growth</h3>
              <p>Strategic accounting & taxation</p>
            </div>
          </div>
          <div class="visual-item">
            <div class="visual-icon">⚙️</div>
            <div class="visual-text">
              <h3>System Integration</h3>
              <p>Seamless ERP solutions</p>
            </div>
          </div>
          <div class="visual-item">
            <div class="visual-icon">🔬</div>
            <div class="visual-text">
              <h3>R&D Claims</h3>
              <p>Maximize research credits</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ════════════════ SERVICES ════════════════ -->
  <section class="services-section">
    <div class="section-header">
      <div class="section-label">Our Expertise</div>
      <h2 class="section-title">Specialized <span>Services</span></h2>
      <p class="section-description">Tailored solutions designed to address your unique business challenges and opportunities.</p>
    </div>

    <div class="services-grid">
      <div class="service-card">
        <div class="service-icon">📊</div>
        <h3>Accounting & Bookkeeping</h3>
        <p>Comprehensive financial management with accuracy and compliance. Real-time reporting, cloud-based systems, and strategic financial planning.</p>
        <a href="#" class="service-link">Learn More →</a>
      </div>

      <div class="service-card">
        <div class="service-icon">💼</div>
        <h3>Taxation & Compliance</h3>
        <p>Expert tax strategy and compliance services. Minimize liabilities, optimize deductions, and ensure full regulatory adherence across jurisdictions.</p>
        <a href="#" class="service-link">Learn More →</a>
      </div>

      <div class="service-card">
        <div class="service-icon">⚙️</div>
        <h3>ERP Integration</h3>
        <p>Seamless enterprise resource planning implementation. Integrate systems, streamline operations, and enhance visibility across your entire organization.</p>
        <a href="#" class="service-link">Learn More →</a>
      </div>

      <div class="service-card">
        <div class="service-icon">📈</div>
        <h3>Business Advisory</h3>
        <p>Strategic guidance for sustainable growth. Data-driven insights, operational optimization, and financial planning to scale efficiently.</p>
        <a href="#" class="service-link">Learn More →</a>
      </div>

      <div class="service-card">
        <div class="service-icon">🔬</div>
        <h3>R&D Claims & Credits</h3>
        <p>Maximize research and development tax relief. Comprehensive claim preparation and compliance to recover eligible R&D investments.</p>
        <a href="#" class="service-link">Learn More →</a>
      </div>

      <div class="service-card">
        <div class="service-icon">🌍</div>
        <h3>International Services</h3>
        <p>Cross-border solutions for global businesses. Multi-jurisdictional compliance, transfer pricing, and international tax optimization strategies.</p>
        <a href="#" class="service-link">Learn More →</a>
      </div>
    </div>
  </section>

  <!-- ════════════════ FEATURES ════════════════ -->
  <section class="features-section">
    <div class="section-header">
      <div class="section-label">Why Choose Us</div>
      <h2 class="section-title">Why Businesses <span>Trust Kanbir</span></h2>
      <p class="section-description">Proven expertise, innovative solutions, and dedicated partnership for your success.</p>
    </div>

    <div class="features-grid">
      <div class="feature-box">
        <div class="feature-number">500+</div>
        <h3>Businesses Served</h3>
        <p>Trusted by companies across UK and international markets. Our extensive portfolio spans diverse industries and business sizes.</p>
      </div>

      <div class="feature-box">
        <div class="feature-number">15+</div>
        <h3>Years Experience</h3>
        <p>Over a decade and a half of proven expertise in financial services, accounting, and business consulting across multiple sectors.</p>
      </div>

      <div class="feature-box">
        <div class="feature-number">99%</div>
        <h3>Satisfaction Rate</h3>
        <p>Exceptional client satisfaction with dedicated support, transparent communication, and measurable results consistently delivered.</p>
      </div>
    </div>
  </section>

  <!-- ════════════════ CTA ════════════════ -->
  <section class="cta-section">
    <div class="cta-content">
      <h2>Ready to Transform Your Business?</h2>
      <p>Get expert insights tailored to your specific needs. Let's discuss how Kanbir Solutions can drive your growth and success.</p>
      
      <div class="cta-buttons">
        <button class="btn primary">Book Free Consultation</button>
        <button class="btn light">View Pricing</button>
      </div>
    </div>
  </section>

  <!-- ════════════════ MOUSE EFFECTS ════════════════ -->
  <div class="mouse-follower"></div>
  <div class="mouse-glow"></div>

  <script>
    // Mouse Follower Effect
    const follower = document.querySelector('.mouse-follower');
    const glow = document.querySelector('.mouse-glow');

    document.addEventListener('mousemove', (e) => {
      follower.classList.add('active');
      glow.classList.add('active');

      // Smooth follower
      follower.style.left = (e.clientX - 15) + 'px';
      follower.style.top = (e.clientY - 15) + 'px';

      // Glow effect
      glow.style.left = (e.clientX - 50) + 'px';
      glow.style.top = (e.clientY - 50) + 'px';
    });

    // Hide on mouse leave
    document.addEventListener('mouseleave', () => {
      follower.classList.remove('active');
      glow.classList.remove('active');
    });

    // Navbar scroll effect
    const navbar = document.querySelector('.navbar');
    window.addEventListener('scroll', () => {
      if (window.scrollY > 10) {
        navbar.style.boxShadow = 'var(--shadow-md)';
      } else {
        navbar.style.boxShadow = 'var(--shadow-sm)';
      }
    });

    // Interactive card effects
    const serviceCards = document.querySelectorAll('.service-card');
    serviceCards.forEach(card => {
      card.addEventListener('mouseenter', function() {
        this.style.transform = 'translateY(-12px) scale(1.02)';
      });
      card.addEventListener('mouseleave', function() {
        this.style.transform = 'translateY(0) scale(1)';
      });
    });
  </script>
</body>
</html>
